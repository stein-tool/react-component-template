import React from 'react';
import ReactDOM from 'react-dom';

import <{%=className %}> from './App.jsx';

const render = () => {
	ReactDOM.render(
		<<{%=className %}> />
		,document.getElementById('root'));
};
render();
